var opbeat = require('../')
  , fs = require('fs')
  , glob = require('glob')
  , path = require('path')
  , should = require('should');

describe('opbeat.utils', function() {
  describe('#parseStack()', function(){
    // needs new tests with a mock callsite object
  });
});
