version: '2.1'

services:
  kafka:
    extends:
      file: docker-compose.yml
      service: kafka
  node_tests:
    extends:
      file: docker-compose-node-test.yml
      service: node_tests
    depends_on:
      kafka:
        condition: service_healthy

volumes:
  nodekafkadata:
    driver: local
